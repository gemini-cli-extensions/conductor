{
  "name": "conductor",
  "version": "0.1.3",
  "contextFileName": "GEMINI.md",
  "mcpServers": {
    "aic": {
      "command": "python3",
      "args": [
        "-c",
        "import sys; sys.path.append('${extensionPath}'); import aic.server; aic.server.mcp.run()"
      ],
      "cwd": "${workspaceFolder}"
    }
  }
}
